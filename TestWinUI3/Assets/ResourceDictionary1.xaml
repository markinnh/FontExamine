<?xml version="1.0" encoding="utf-8"?>
<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TestWinUI3.ViewModel"
    xmlns:markup="using:TestWinUI3.Markup">
<Style x:Key="MyButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="LightBlue"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="Padding" Value="10,5"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="CornerRadius" Value="5"/>
        <Setter Property="Height" Value="40"/>
        <Setter Property="Width" Value="40"/>
        <Setter Property="FontFamily" Value="Segoe Fluent Icons"/>
        <Setter Property="Margin" Value="5,5,0,0"/>
    </Style>
    <DataTemplate x:Key="ClickNotificationTemplate">
        <StackPanel Orientation="Horizontal" Margin="5">
            
            <TextBlock Text="{Binding Message}" FontSize="14" VerticalAlignment="Center" FontFamily="Segoe Fluent Icons"/>
            <TextBlock Text="{Binding Timestamp}" FontSize="12" Foreground="Gray" Margin="10,0,0,0" VerticalAlignment="Center"/>
        </StackPanel>
    </DataTemplate>
</ResourceDictionary>
