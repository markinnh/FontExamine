<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:FontExamine">
    <Style TargetType="{x:Type local:OldTokenizedTextBox}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:OldTokenizedTextBox}">
                    <Grid>
                        <!-- Editable text layer -->
                        <TextBox x:Name="PART_TextBox"
                             Text="{Binding DisplayText, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                             Background="Transparent"
                             BorderThickness="0"
                             FontSize="16"
                             AcceptsReturn="True"
                             VerticalScrollBarVisibility="Auto"
                             HorizontalScrollBarVisibility="Auto"
                             TextWrapping="Wrap"/>

                        <!-- Token overlay layer -->
                        <ItemsControl x:Name="PART_ItemsControl"
                                  ItemsSource="{Binding Tokens, RelativeSource={RelativeSource TemplatedParent}}"
                                  IsHitTestVisible="False">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <Canvas />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>

                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#FFCCE5FF"
                                        BorderBrush="#FF5599FF"
                                        BorderThickness="1"
                                        CornerRadius="4"
                                        Padding="4,1"
                                        SnapsToDevicePixels="True">
                                        <TextBlock Text="{Binding}" 
                                               FontWeight="Bold"
                                               Foreground="#FF003366"/>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style TargetType="{x:Type local:TokenizedTextBox}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:TokenizedTextBox}">
                    <Border Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">

                        <AdornerDecorator>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <Popup x:Name="PART_AutoCompletePopup"
                                        Placement="Relative"
                                        StaysOpen="False"
                                        AllowsTransparency="True">
                                    <Border Background="White" BorderBrush="Gray" BorderThickness="1">
                                        <ListBox x:Name="PART_AutoCompleteList"
                                                 MinWidth="120"
                                                 MaxHeight="200"
                                                 Focusable="False"/>
                                    </Border>
                                </Popup>
                                <TextBox x:Name="PART_TextBox" Grid.Row="0"
                                 Text="{Binding Text, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                 AcceptsReturn="True"
                                 TextWrapping="Wrap"
                                 VerticalScrollBarVisibility="Auto"
                                 HorizontalScrollBarVisibility="Auto"/>
                            </Grid>
                        </AdornerDecorator>

                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
</ResourceDictionary>